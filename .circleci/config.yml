on: 2
jobs:
  build:
    docker:
      - image: microsoft/dotnet:2-sdk
      
    working_directory: ~/repo

    steps:
      - checkout
      - run: dotnet restore
      - run:
          name: Buidl
          command: dotnet build
      - run: docker build -t Lab2 .

